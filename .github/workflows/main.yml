name: Main

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  id-token: write
  contents: read

env:
    AWS_REGION: ap-southeast-2
#    AWS_PROFILE:

jobs:
  aws:

    runs-on: ubuntu-latest

    steps:
    - name: Git clone the repository
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      if: ${{ !env.ACT }}
      uses: aws-actions/configure-aws-credentials@v1.7.0
      with:
        role-to-assume: arn:aws:iam::236281372978:role/GitHubAction-AssumeRoleWithAction
        role-session-name: GitHub_to_AWS_via_FederatedOIDC
        aws-region: ${{ env.AWS_REGION }}

    - name: Configure AWS credentials
      if: ${{ env.ACT }}
      uses: aws-actions/configure-aws-credentials@v1.7.0
      with:
        aws-region: ${{ env.AWS_REGION }}

    - name: Sts - GetCallerIdentity
      run: |
        aws --no-cli-pager sts get-caller-identity

    - name: Lambda - List Functions
      run: |
        aws --no-cli-pager lambda list-functions

#    - name: Configure aws credentials
#      uses: aws-actions/configure-aws-credentials@v1.7.0
#      with:
#        role-to-assume: arn:aws:iam::680536935516:role/GitHubAction-AssumeRoleWithAction
#        role-session-name: GitHub_to_AWS_via_FederatedOIDC
#        aws-region: ${{ env.AWS_REGION }}
#
#    - name: Sts GetCallerIdentity
#      run: |
#        aws sts get-caller-identity
#
#    - name: Copy files to the test website with the AWS CLI
#      run: |
#        aws lambda list-functions
#  native:
#
#    runs-on: ubuntu-latest
#
#    steps:
#    - uses: actions/checkout@v4
#
#    - uses: graalvm/setup-graalvm@v1
#      with:
#        java-version: '21'
#        distribution: 'graalvm' # See 'Options' for all available distributions
#        github-token: ${{ secrets.GITHUB_TOKEN }}
#
#    - name: Example step
#      run: |
#        echo "GRAALVM_HOME: $GRAALVM_HOME"
#        echo "JAVA_HOME: $JAVA_HOME"
#        java --version
#        native-image --version


  gradle:
    needs: aws

#    strategy:
#      matrix:
#        os: [ubuntu-latest, macos-latest, windows-latest]
#    runs-on: ${{ matrix.os }}

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-java@v3
      with:
        distribution: temurin
        java-version: 17

    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2

    - name: Execute Gradle - buildNativeLambda
      run: ./gradlew :app:buildNativeLambda

    - name: Execute Gradle - test
      run: ./gradlew :app:test

    - name: Set up Node
      uses: actions/setup-node@v3
      with:
        node-version: "21"

#    - name: Set up JDK 17
#      uses: actions/setup-java@v3
#      with:
#        java-version: '17'
#        distribution: 'graalvm'
#    - name: Build with Gradle
#      uses: gradle/gradle-build-action@bd5760595778326ba7f1441bcf7e88b49de61a25 # v2.6.0
#      with:
#        arguments: :app:buildNativeLambda
